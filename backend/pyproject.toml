[tool.pytest.ini_options]
pythonpath = "."
testpaths = ["tests"]
markers = [
    "integration: Tests, die eine laufende Postgres-Test-DB brauchen (Auto-Skip ohne Server).",
    "sandbox: Tests, die je einen Sandbox-Prozess starten (langsam, aber die Isolationsgarantie haengt daran).",
]
